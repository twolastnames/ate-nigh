"use client";

import { testHost } from "../../cypress/helpers";

describe('<%= downName %>', () => {
    it('can look', () => {
        cy.visit(`${testHost}/<%= downName %>`)
        cy.contains('<%= downName %>')
    })
});

