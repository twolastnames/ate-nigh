"use client"
import React from "react";
import { render, screen } from "@testing-library/react";
import { <%= upName %> } from "../<%= upName %>"

test('work', () => {
    render(< <%= upName %> />)
    expect(screen.getByTestId("<%= upName %>")).toMatchSnapshot()
});
